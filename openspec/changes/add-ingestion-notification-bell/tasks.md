## 1. 通知领域与事件映射

- [x] 1.1 设计通知数据模型（类型、标题、内容、关联资源、已读状态、创建时间）
- [x] 1.2 定义文档入库状态到通知事件的映射：`completed`、`failed`、`stalled/timeout`
- [x] 1.3 在多租户约束下补齐 user_id 隔离与索引策略（未读计数、时间倒序查询）

## 2. 后端接口与服务

- [x] 2.1 新增通知列表接口（`page/page_size`，默认按时间倒序）
- [x] 2.2 新增未读计数接口（用于铃铛红点展示）
- [x] 2.3 新增标记已读接口（按 `notification_ids` 批量标记）
- [x] 2.4 在 ingestion job 状态流转处发布通知并保证幂等（避免重复通知）
- [x] 2.5 新增超时阈值配置项 `KB_INGESTION_NOTIFICATION_TIMEOUT_SECONDS`（默认 600 秒）

## 3. 前端铃铛交互改造

- [x] 3.1 将头部铃铛改为可展开通知面板，展示最近通知列表
- [x] 3.2 红点/计数与未读状态绑定，点击通知项后标记该条已读
- [x] 3.3 通知项点击跳转：`completed` 到文档分块页，`failed/timeout` 到知识库详情并定位文档
- [x] 3.4 隐藏模型连通性展示块，避免头部拥挤与认知噪音

## 4. 刷新策略与体验兜底

- [x] 4.1 MVP 采用前端定时轮询（例如 15-30 秒）拉取通知与未读计数
- [x] 4.2 明确空态、加载态、错误态文案，保证铃铛在网络异常下可恢复
- [x] 4.3 预留后续 SSE/WebSocket 实时推送扩展点（不在本次实现）

## 5. 测试与验收

- [x] 5.1 后端测试：事件生成、未读统计、已读更新、租户隔离、幂等防重
- [x] 5.2 前端测试：铃铛红点、面板列表渲染、点击跳转、轮询刷新
- [x] 5.3 验收场景：上传大文件后离开页面，处理完成/失败后可通过铃铛收到提醒并一跳直达
- [x] 5.4 验收场景：同一 ingestion job 的 timeout 通知在 30 分钟窗口内最多生成 1 条
