## 1. 异步入库基础设施

- [x] 1.1 设计并创建 `ingestion_jobs` 数据模型与 Alembic 迁移（含状态、重试、错误、时间戳字段）
- [x] 1.2 实现 ingestion job repository/service，统一生命周期状态流转与幂等更新
- [x] 1.3 将文档上传接口改为“接单即返回”：落盘、`document=queued`、创建 job、投递 Celery
- [x] 1.4 实现 Celery 文档入库任务执行器，迁移现有同步 ingest 逻辑并补充异常兜底
- [x] 1.5 增加文档/任务状态查询接口或扩展现有接口字段，支持前端轮询订阅

## 2. pgvector 原生检索升级

- [ ] 2.1 迁移 `kb_vector_chunks` 向量存储到 pgvector 原生列并处理历史数据兼容
- [ ] 2.2 为 PostgreSQL 检索路径增加向量索引与查询语句（top_k、阈值过滤、排序）
- [ ] 2.3 重构 `PostgresPgVectorConnector.search`，移除 Python 内存相似度主路径
- [ ] 2.4 为维度不一致场景补充严格校验与错误信息（入库与查询两侧）

## 3. 检索响应契约与策略框架

- [ ] 3.1 扩展 retrieval schema，新增命中血缘字段与策略快照字段并保持向后兼容
- [ ] 3.2 定义 `RetrievalStrategy` 抽象接口与策略注册机制
- [ ] 3.3 将现有 `retrieve` 主流程迁移到 `naive.v1` 策略实现并验证行为等价
- [ ] 3.4 增加 `strategy_id` 校验与错误处理，拒绝未知策略并返回可诊断信息

## 4. 前端与交互改造

- [x] 4.1 知识库详情页上传交互改为“已入队”语义并展示异步处理中状态
- [x] 4.2 实现文档状态轮询（或预留 SSE 接口）并处理失败/重试提示
- [x] 4.3 升级试检索结果展示，渲染血缘字段与策略/参数快照信息
- [x] 4.4 新增文档“查看分块”入口（仅 `completed` 可用），展示 chunk 统计与分页明细
- [x] 4.5 新增后端 chunk 明细查询接口并补齐多租户权限校验

## 5. 测试、可观测性与发布

- [ ] 5.1 新增后端单测/集成测试覆盖：异步入库状态机、失败重试、pgvector 检索、策略选择
- [ ] 5.2 新增前端测试覆盖：上传入队提示、状态刷新、检索新字段展示
- [ ] 5.3 增加任务与检索关键指标日志（入库耗时、失败率、队列积压、检索延迟）
- [ ] 5.4 制定发布与回滚方案：迁移顺序、灰度开关、故障回退路径

## 6. 文档分块查看 MVP 验收清单（DoD）

- [x] 6.1 文档列表页仅在 `document.status=completed` 时启用“查看分块”按钮，其它状态展示禁用提示
- [x] 6.2 `GET /knowledge-bases/{kb_id}/documents/{document_id}/chunks` 返回分页结构与统计字段，默认按 `chunk_index ASC`
- [x] 6.3 文档状态非 `completed` 时返回 `409 document_not_ready`，前端展示一致文案并支持返回列表
- [x] 6.4 非本人文档访问统一返回 `404`（或项目约定的等效策略），通过测试验证不存在租户越权
- [x] 6.5 分块表格字段渲染符合映射草案：`preview_text` 截断、`token_length=null` 显示 `-`、`source` 缺失可兜底
- [x] 6.6 空数据场景（`total_chunks=0`）展示空态，不报错、不阻断页面
- [x] 6.7 分页参数边界（page<1、page_size>max、非法类型）返回 `422` 且错误信息可诊断
- [x] 6.8 至少补齐 1 组后端接口测试 + 1 组前端交互测试覆盖上述关键路径
